export const SVG_WIDTH = 520;
export const SVG_HEIGHT = 420;
export const GRID_SIZE = 40;
export const MAX_INPUT_DIMENSION = 9;
export const EDGE_LABEL_OFFSET = 8;

export const workspaceBounds = {
  x: 20,
  y: 20,
  width: 480,
  height: 360,
};

export const maxWorkspaceColumns = workspaceBounds.width / GRID_SIZE;
export const maxWorkspaceRows = workspaceBounds.height / GRID_SIZE;
