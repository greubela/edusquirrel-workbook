# Optional: development-only controls in the UI.
# Default: visible during `npm run dev`, hidden in production builds.
# Set this explicitly to "true" or "false" to override the default.
VITE_SHOW_DEV_CONTROLS=true
